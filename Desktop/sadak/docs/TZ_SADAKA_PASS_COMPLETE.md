# 📋 Техническое задание: Раздел "Садака-Пасс" (Полная версия)

## 🎯 Общая концепция

Раздел "Садака-Пасс" представляет собой комплексную платформу для благотворительных пожертвований (садака, закят) с двумя основными бизнес-моделями:

1. **Классическое донорство** — пожертвования в готовые фонды (единоразовые и регулярные подписки)
2. **Целевые кампании (Crowd-Sadaqa)** — пользователи создают собственные инициативы и привлекают других доноров

---

## 📱 Структура раздела (вкладки Mini App и inline-режим)

### Основные вкладки навигации:

```
Пожертвовать | Поддержать | Целевые кампании | Подписка | Закят | История | Фонды-партнёры
```

**Порядок и группировка:**
- **Донорство в фонды:** Пожертвовать, Поддержать, Подписка
- **Целевые инициативы:** Целевые кампании
- **Обязательные выплаты:** Закят
- **Информация:** История, Фонды-партнёры

---

## 🔹 Раздел 1: Пожертвовать

### 1.1. Цель и функциональность

Позволить пользователю сделать единоразовое пожертвование в один из проверенных фондов-партнёров.

### 1.2. UX-поток

#### A. Главный экран

1. **Список фондов** с карточками:
   - Логотип фонда
   - Название фонда
   - Краткое описание (1-2 строки)
   - Бейдж "✓ Проверено" (если фонд верифицирован)
   - Кнопка "Помочь" / "Пожертвовать"

2. **Фильтры:**
   - По категории (мечеть, сироты, обучение, помощь нуждающимся, медицина, иное)
   - По стране (RU, KZ, UZ, и т.д.)
   - По статусу верификации (все / только проверенные)

3. **Поиск:**
   - По названию фонда
   - По описанию

#### B. Страница фонда и выбор суммы

1. **Детальная информация о фонде:**
   - Полное описание
   - Категория, страна
   - Ссылка на сайт фонда (если есть)
   - История пожертвований (сколько собрано через платформу)

2. **Выбор суммы:**
   - Пресеты: 100, 250, 500, 1000, 2500 ₽
   - Поле для произвольной суммы
   - Минимальная сумма: 50 ₽

3. **Кнопка "Пожертвовать":**
   - Переход на платежную страницу

#### C. Оплата

1. **Выбор платёжной системы:**
   - YooKassa (для РФ)
   - CloudPayments (международные платежи)

2. **После успешной оплаты:**
   - Экран благодарности
   - Кнопка "Сделать регулярным" (переход в раздел Подписка)
   - Кнопка "Расскажите друзьям" (шаринг)
   - Кнопка "Перейти в историю"

### 1.3. API Endpoints

```
GET  /api/v1/funds
     Query: country_code?, category?, verified?
     Response: List[Fund]

GET  /api/v1/funds/{id}
     Response: Fund

POST /api/v1/donations/init
     Body: { fund_id, amount_value, currency, donation_type: "sadaqa" }
     Response: { id, payment_url, status }

GET  /api/v1/donations/{id}
     Response: Donation
```

### 1.4. Модель данных

**Fund:**
```python
{
    id: int
    name: str
    description: str
    category: str
    country_code: str
    logo_url: str?
    website_url: str?
    verified: bool
    created_at: datetime
}
```

---

## 🔹 Раздел 2: Поддержать

### 2.1. Цель и функциональность

Быстрое пожертвование на развитие платформы MubarakWay без выбора фонда.

### 2.2. UX-поток

1. **Выбор суммы:**
   - Пресеты: 500, 1000, 2500, 5000 ₽
   - Поле для произвольной суммы
   - Минимум: 100 ₽

2. **Кнопка "Поддержать":**
   - Переход на оплату

3. **После оплаты:**
   - Карточка благодарности
   - Кнопка "Расскажите друзьям"

### 2.3. API Endpoints

```
POST /api/v1/donations/init
     Body: { amount_value, currency, donation_type: "support" }
     Response: { id, payment_url, status }
```

---

## 🔹 Раздел 3: Целевые кампании (Crowd-Sadaqa)

### 3.1. Цель и функциональность

Позволить любому пользователю создать собственную целевую кампанию и привлечь других доноров. Это аналог crowdfunding внутри MubarakWay с исламской этикой (без процентов, запрещённой рекламы, комиссий вне дозволенного).

### 3.2. UX-потоки

#### A. Просмотр активных кампаний

**Экран списка кампаний:**

1. **Карточки кампаний:**
   - Фото / баннер кампании
   - Заголовок (название цели)
   - Краткое описание (1-2 строки)
   - Категория (мечеть, сироты, обучение, иное)
   - Прогресс-бар: собрано / цель
   - Счётчик участников
   - Бейдж "✓ Проверено" (если прошла модерацию)
   - Бейдж статуса: "Активна", "Завершена", "На модерации"
   - Счётчик дней до завершения (если активна)
   - Кнопки: "Присоединиться" / "Подробнее"

2. **Фильтры:**
   - По категории
   - По стране
   - По статусу (активные, завершённые, на модерации)

3. **Поиск:**
   - По названию
   - По описанию

4. **Сортировка:**
   - По дате создания (новые сначала)
   - По прогрессу (% сбора)
   - По популярности (участники)

#### B. Создание кампании

**Форма создания:**

1. **Кнопка "Создать свою цель"** на главной странице кампаний

2. **Обязательные поля:**
   - Название цели* (макс. 255 символов)
   - Краткое описание* (макс. 2000 символов)
   - Категория* (выбор из списка)
   - Сумма цели* (минимум 1000 ₽)
   - Фонд-получатель* (выбор из списка фондов-партнёров)
   - Срок сбора* (дата завершения, минимум +7 дней от текущей)

3. **Опциональные поля:**
   - Фото / баннер (загрузка изображения, макс. 5 МБ)

4. **Чекбоксы согласий:**
   - Согласие с правилами платформы
   - Согласие на публичное размещение кампании
   - Согласие предоставить отчёт после завершения

5. **Процесс модерации:**
   - После отправки форма → статус "На проверке" (pending)
   - Админ проверяет кампанию
   - После одобрения → статус "Активна" → публикация в каталоге
   - При отклонении → статус "Отклонена" + причина отклонения

#### C. Детальная страница кампании

**Экран кампании:**

1. **Заголовок и описание:**
   - Полное описание цели
   - Категория, автор кампании
   - Фонд-получатель

2. **Прогресс сбора:**
   - Визуальный прогресс-бар (%)
   - Собрано / Цель
   - Счётчик участников

3. **Форма пожертвования:**
   - Пресеты: 500, 1000, 2500, 5000 ₽
   - Произвольная сумма (минимум 100 ₽)
   - Кнопка "Присоединиться"

4. **Дополнительно:**
   - Кнопка шаринга (ShareButton)
   - История пожертвований (последние 10)
   - Вкладка "Отчёт" (для завершённых кампаний)

5. **Отчёт о завершении:**
   - Статус "Завершена"
   - Итоговая сумма сбора
   - Куда перечислены средства (название фонда, ссылка на отчёт фонда)
   - Фото/документы (акт, чек)

#### D. Присоединение к кампании

1. **Выбор суммы** (аналогично обычному пожертвованию)

2. **Оплата:**
   - Переход на платежную систему
   - `campaign_id` передаётся в запросе

3. **После успешной оплаты:**
   - Обновление прогресс-бара кампании
   - Уведомление организатору (опционально)
   - Благодарность донору

#### E. Завершение кампании

**Автоматическое завершение при:**
- Достижении целевой суммы
- Истечении срока сбора

**Процесс:**
1. Статус меняется на "Завершена"
2. Автор получает уведомление
3. Автоматический отчёт:
   - Сколько собрано
   - Куда перечислено (фонд/агент)
   - Ссылка на отчёт фонда (если есть)
4. Публикация отчёта в карточке кампании

### 3.3. API Endpoints

```
GET  /api/v1/campaigns
     Query: status?, country_code?, category?
     Response: List[Campaign]

GET  /api/v1/campaigns/{id}
     Response: Campaign

POST /api/v1/campaigns
     Body: CampaignCreate
     Response: Campaign (status: pending)

POST /api/v1/campaigns/{id}/donate
     Body: { amount_value, currency }
     Response: { id, payment_url, status }

GET  /api/v1/campaigns/{id}/report
     Response: CampaignReport (для завершённых)

PATCH /api/v1/campaigns/{id}/status
     Body: { status: "approved"|"rejected", rejection_reason? }
     Response: Campaign
     (Админ-эндпоинт для модерации)
```

### 3.4. Модель данных

**Campaign:**
```python
{
    id: int
    owner_id: int
    fund_id: int
    title: str
    description: str
    category: str?
    goal_amount: Decimal
    collected_amount: Decimal
    currency: str
    banner_url: str?
    start_date: datetime
    end_date: datetime
    status: "draft" | "pending" | "active" | "completed" | "expired" | "rejected" | "cancelled"
    moderated_by: int?
    moderated_at: datetime?
    rejection_reason: str?
    participants_count: int
    created_at: datetime
    updated_at: datetime?
}
```

**CampaignReport:**
```python
{
    campaign_id: int
    total_collected: Decimal
    total_participants: int
    fund_name: str
    fund_report_url: str?
    transferred_at: datetime?
    report_documents: List[str]?  # URLs to PDF/images
}
```

### 3.5. Админ-панель (модерация)

**Эндпоинты:**
```
GET  /api/v1/admin/campaigns/pending
     Response: List[Campaign]

POST /api/v1/admin/campaigns/{id}/approve
     Body: {}
     Response: Campaign

POST /api/v1/admin/campaigns/{id}/reject
     Body: { rejection_reason: str }
     Response: Campaign
```

---

## 🔹 Раздел 4: Подписка (Садака-джария)

### 4.1. Цель и функциональность

Регулярные автоматические пожертвования в фонды-партнёры (подписка).

### 4.2. UX-поток

1. **Выбор тарифа:**
   - Базовый: 500 ₽/мес
   - Pro: 1000 ₽/мес (+ 5% в благотворительность)
   - Premium: 2500 ₽/мес (+ 10% в благотворительность)

2. **Выбор периода:**
   - 1 месяц
   - 3 месяца (+ 2 мес. в подарок)
   - 6 месяцев (+ 2 мес. в подарок)
   - 12 месяцев (+ 4 мес. в подарок)

3. **Выбор фонда (опционально):**
   - Можно привязать к конкретному фонду
   - Или оставить общее пожертвование

4. **Оплата:**
   - Рекуррентный платёж (сохранение токена карты)

5. **Управление:**
   - Просмотр активных подписок
   - Отмена подписки
   - Пауза / возобновление (опционально)

### 4.3. API Endpoints

```
POST /api/v1/subscriptions/init
     Body: { plan, period, fund_id?, amount_value }
     Response: { id, payment_url, status }

GET  /api/v1/subscriptions
     Response: List[Subscription]

POST /api/v1/subscriptions/{id}/cancel
     Response: Subscription

PATCH /api/v1/subscriptions/{id}
     Body: { status: "paused" | "active" }
     Response: Subscription
```

### 4.4. Модель данных

**Subscription:**
```python
{
    id: int
    user_id: int
    fund_id: int?
    plan: "basic" | "pro" | "premium"
    period: "P1M" | "P3M" | "P6M" | "P12M"
    amount_value: Decimal
    currency: str
    charity_percent: Decimal?  # 5% для Pro, 10% для Premium
    status: "active" | "paused" | "cancelled" | "expired"
    payment_token: str?
    payment_provider: str?
    started_at: datetime
    next_charge_at: datetime
    expires_at: datetime
    created_at: datetime
}
```

---

## 🔹 Раздел 5: Закят

### 5.1. Цель и функциональность

Калькулятор обязательного закята (2.5% от накоплений) и выплата рассчитанного закята.

### 5.2. UX-поток

1. **Калькулятор:**
   - Формы для расчёта:
     - Деньги (наличные, на счетах)
     - Золото/серебро (граммы)
     - Товары для продажи
     - Инвестиции
     - Долги (которые должны вернуть)
   - Автоматический расчёт нисаба (минимальная сумма)
   - Кнопка "Рассчитать закят"
   - Отображение суммы закята

2. **Выплата:**
   - Кнопка "Выплатить закят"
   - Выбор фонда
   - Оплата

3. **История:**
   - Сохранение всех расчётов
   - История выплат

### 5.3. API Endpoints

```
POST /api/v1/zakat/calc
     Body: ZakatCalcCreate
     Response: { total_zakat, breakdown }

POST /api/v1/zakat/pay
     Body: { calculation_id, fund_id }
     Response: { id, payment_url, status }

GET  /api/v1/zakat/history
     Response: List[ZakatCalc]
```

### 5.4. Модель данных

**ZakatCalc:**
```python
{
    id: int
    user_id: int
    cash_amount: Decimal?
    gold_grams: Decimal?
    silver_grams: Decimal?
    goods_value: Decimal?
    investments_value: Decimal?
    debts_receivable: Decimal?
    total_wealth: Decimal
    nisab_value: Decimal
    zakat_amount: Decimal
    donation_id: int?
    created_at: datetime
}
```

---

## 🔹 Раздел 6: История

### 6.1. Цель и функциональность

История всех транзакций пользователя и статистика пожертвований.

### 6.2. UX-поток

1. **Список транзакций:**
   - Дата
   - Тип операции (пожертвование, подписка, закят, кампания)
   - Фонд / Кампания
   - Сумма
   - Статус (завершена, в обработке, отменена)
   - Ссылка на чек/отчёт (если есть)

2. **Фильтры:**
   - По периоду (сегодня, неделя, месяц, год, всё время)
   - По типу операции
   - По фонду/кампании
   - По статусу

3. **Статистика:**
   - Общая сумма пожертвований
   - Количество транзакций
   - Топ фонды/кампании
   - График пожертвований (опционально)

### 6.3. API Endpoints

```
GET  /api/v1/me/history
     Query: type?, period?, fund_id?, campaign_id?
     Response: List[TransactionHistory]

GET  /api/v1/me/stats
     Response: { total_donated, total_transactions, top_funds, top_campaigns }
```

---

## 🔹 Раздел 7: Фонды-партнёры

### 7.1. Цель и функциональность

Каталог всех фондов-партнёров с возможностью стать партнёром.

### 7.2. UX-поток

1. **Список фондов:**
   - Карточки с логотипом, названием, описанием
   - Бейдж "✓ Проверено"
   - Кнопка "Пожертвовать"

2. **Фильтры:**
   - По категории
   - По стране

3. **Блок "Хотите стать партнёром?":**
   - Форма заявки:
     - Название организации*
     - Контакты*
     - Категория*
     - Страна*
     - Описание деятельности*
     - Сайт/соцсети

### 7.3. API Endpoints

```
GET  /api/v1/partners/funds
     Query: country_code?, category?
     Response: List[Fund]

POST /api/v1/partners/applications
     Body: PartnerApplicationCreate
     Response: { id, status: "pending" }
```

---

## 🔹 Раздел 8: Backend API (общее)

### 8.1. Аутентификация

Все эндпоинты (кроме публичных) требуют заголовок:
```
X-Telegram-Init-Data: <telegram_web_app_init_data>
```

Или:
```
Authorization: Bearer <token>
```

### 8.2. Общие модели

**Donation:**
```python
{
    id: int
    user_id: int
    fund_id: int?
    campaign_id: int?
    amount_value: Decimal
    currency: str
    status: "pending" | "processing" | "completed" | "failed" | "cancelled"
    provider: "yookassa" | "cloudpayments"
    payment_id: str?
    payment_url: str?
    donation_type: "sadaqa" | "zakat" | "campaign" | "support"
    created_at: datetime
    completed_at: datetime?
}
```

**User:**
```python
{
    id: int
    tg_id: int
    first_name: str?
    last_name: str?
    username: str?
    created_at: datetime
}
```

### 8.3. Webhooks платёжных систем

```
POST /api/v1/payments/webhook/yookassa
     Body: YooKassa webhook payload

POST /api/v1/payments/webhook/cloudpayments
     Body: CloudPayments webhook payload
```

Обработка:
- Обновление статуса пожертвования
- Обновление `collected_amount` в кампании (если есть)
- Увеличение `participants_count` в кампании
- Синхронизация с e-replika.ru (фоново)

---

## 🔹 Раздел 9: Модели данных (полная структура)

### 9.1. Таблицы БД

**users**
- id, tg_id, first_name, last_name, username, created_at

**funds**
- id, name, description, category, country_code, logo_url, website_url, verified, created_at

**campaigns**
- id, owner_id, fund_id, title, description, category, goal_amount, collected_amount, currency, banner_url, start_date, end_date, status, moderated_by, moderated_at, rejection_reason, participants_count, created_at, updated_at

**donations**
- id, user_id, fund_id, campaign_id, amount_value, currency, status, provider, payment_id, payment_url, donation_type, created_at, updated_at, completed_at

**subscriptions**
- id, user_id, fund_id, plan, period, amount_value, currency, charity_percent, status, payment_token, payment_provider, started_at, next_charge_at, expires_at, cancelled_at, created_at, updated_at

**zakat_calculations**
- id, user_id, cash_amount, gold_grams, silver_grams, goods_value, investments_value, debts_receivable, total_wealth, nisab_value, zakat_amount, donation_id, created_at

**reports**
- id, fund_id, campaign_id?, report_type, report_url, report_data, created_at

**partner_applications**
- id, organization_name, contact_email, contact_phone, category, country_code, description, website_url, status, created_at, reviewed_at, reviewed_by

### 9.2. Связи (Foreign Keys)

- donations.user_id → users.id
- donations.fund_id → funds.id
- donations.campaign_id → campaigns.id
- campaigns.owner_id → users.id
- campaigns.fund_id → funds.id
- subscriptions.user_id → users.id
- subscriptions.fund_id → funds.id
- zakat_calculations.user_id → users.id
- zakat_calculations.donation_id → donations.id

---

## 🔹 Раздел 10: Аналитика и события

### 10.1. События для отслеживания

**Создание:**
- `campaign_created` — создана кампания
- `donation_initiated` — инициировано пожертвование
- `subscription_created` — создана подписка

**Модерация:**
- `campaign_approved` — кампания одобрена
- `campaign_rejected` — кампания отклонена

**Транзакции:**
- `donation_completed` — пожертвование завершено
- `campaign_joined` — пользователь присоединился к кампании
- `campaign_completed` — кампания завершена (достигнута цель)

**Подписки:**
- `subscription_renewed` — подписка продлена
- `subscription_cancelled` — подписка отменена

### 10.2. Метрики

- Количество активных кампаний
- Общая сумма собранных средств
- Количество участников кампаний
- Конверсия создания → одобрения кампаний
- Средний размер пожертвования

---

## 🔹 Раздел 11: Безопасность и комплаенс

### 11.1. Аутентификация

- Валидация `X-Telegram-Init-Data` через HMAC
- Проверка подписи Telegram WebApp

### 11.2. Модерация контента

- Все кампании проходят модерацию перед публикацией
- Верификация фондов-партнёров
- Проверка документов для крупных сборов (> 15 000 ₽)

### 11.3. Ограничения

- Минимальная сумма пожертвования: 50 ₽
- Минимальная сумма цели кампании: 1 000 ₽
- Максимальный срок кампании: 365 дней
- Максимальный размер файла баннера: 5 МБ

### 11.4. Прозрачность

- Публичные отчёты о завершённых кампаниях
- История всех транзакций для пользователя
- Связь кампаний с фондами-получателями

---

## 🔹 Раздел 12: Интеграция с e-replika.ru

### 12.1. Синхронизация данных

Автоматическая синхронизация (фоново) следующих сущностей:
- Пользователи (User)
- Пожертвования (Donation)
- Кампании (Campaign)

### 12.2. Endpoints синхронизации

Используется внутренний сервис `e_replika_service` для отправки данных на e-replika.ru.

---

## 🔹 Раздел 13: Платежные интеграции

### 13.1. YooKassa (для РФ)

- Приём платежей по картам
- Рекуррентные платежи для подписок
- Webhook для обновления статусов

### 13.2. CloudPayments (международные)

- Приём международных платежей
- Рекуррентные платежи
- Webhook для обновления статусов

### 13.3. Логика выбора провайдера

- По умолчанию: YooKassa (для RUB)
- CloudPayments: для других валют или по выбору пользователя
- BIN-проверка карты (опционально) для автоматического выбора

---

## 📊 Итоговая структура вкладок

```
┌─────────────────────────────────────────────────┐
│  Пожертвовать | Поддержать | Целевые кампании    │
│  Подписка | Закят | История | Фонды-партнёры     │
└─────────────────────────────────────────────────┘
```

**Описание вкладок:**
1. **Пожертвовать** — единоразовые пожертвования в фонды
2. **Поддержать** — быстрая поддержка платформы
3. **Целевые кампании** — создание и участие в кампаниях
4. **Подписка** — регулярные автоматические пожертвования
5. **Закят** — калькулятор и выплата закята
6. **История** — история транзакций и статистика
7. **Фонды-партнёры** — каталог фондов и заявки на партнёрство

---

## 🎯 Ключевые особенности реализации

### Двухуровневая бизнес-модель:

1. **Классическое донорство:**
   - Пожертвовать → выбор фонда → оплата
   - Подписка → регулярные платежи в фонд

2. **Целевые кампании (Crowd-Sadaqa):**
   - Создание кампании пользователем → модерация → публикация
   - Привлечение доноров → сбор средств → отчёт

### Прозрачность:

- Все транзакции видны в истории
- Отчёты по завершённым кампаниям
- Связь с фондами-получателями

### Модерация:

- Все кампании проходят проверку перед публикацией
- Админ может одобрить или отклонить
- Причина отклонения передаётся автору

---

## ✅ Чек-лист реализации MVP

### Frontend:
- [x] Пожертвовать (DonatePage)
- [x] Поддержать (SupportPage)
- [x] Целевые кампании (CampaignsPage, CampaignDetailPage)
- [x] Подписка (SubscriptionPage)
- [x] Закят (ZakatPage)
- [x] История (HistoryPage)
- [x] Фонды-партнёры (PartnersPage)

### Backend:
- [x] API для фондов
- [x] API для пожертвований
- [x] API для кампаний
- [x] API для подписок
- [x] API для закята
- [x] API для истории
- [x] Webhooks платёжных систем
- [x] Админ-эндпоинты для модерации

### Модели данных:
- [x] User
- [x] Fund
- [x] Campaign
- [x] Donation
- [x] Subscription
- [x] ZakatCalc
- [ ] PartnerApplication (требуется создание)

### Интеграции:
- [x] YooKassa
- [x] CloudPayments
- [x] e-replika.ru синхронизация

---

**Дата создания:** 2025-01-27  
**Версия:** 1.0 (Полная интеграция с целевыми кампаниями)

