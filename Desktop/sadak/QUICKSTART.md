# 🚀 Быстрый старт проекта Садака-Пасс

## Предварительные требования

- Python 3.11+
- Node.js 18+
- PostgreSQL 14+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Шаг 1: Клонирование и настройка

```bash
# Перейдите в директорию проекта
cd sadak
```

## Шаг 2: Backend

### Установка и настройка

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env (скопируйте из .env.example)
# Заполните необходимые переменные
cp .env.example .env
```

### Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb sadakadb

# Обновите DATABASE_URL в .env
# DATABASE_URL=postgresql://user:password@localhost:5432/sadakadb

# Примените миграции
alembic upgrade head
```

### Запуск backend

```bash
uvicorn app.main:app --reload
```

Backend будет доступен по адресу: http://localhost:8000
API документация: http://localhost:8000/docs

## Шаг 3: Frontend

### Установка и настройка

```bash
cd frontend

# Установите зависимости
npm install

# Создайте файл .env
echo "VITE_API_URL=http://localhost:8000/api/v1" > .env
```

### Запуск frontend

```bash
npm run dev
```

Frontend будет доступен по адресу: http://localhost:3000

## Шаг 4: Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен в `.env` как `TELEGRAM_BOT_TOKEN`

2. Настройте Mini App:
   - Отправьте `/newapp` в @BotFather
   - Выберите вашего бота
   - Укажите URL фронтенда (для разработки можно использовать ngrok: `ngrok http 3000`)
   - Загрузите иконку

3. Получите Secret Key:
   - Перейдите в настройки бота → Advanced → Secret Key
   - Сохраните в `.env` как `TELEGRAM_SECRET_KEY`

## Шаг 5: Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку "Menu" → выберите ваше Mini App
3. Проверьте работу всех разделов

## Структура проекта

```
sadak/
├── backend/          # FastAPI приложение
│   ├── app/
│   │   ├── api/      # API роутеры
│   │   ├── models/   # Модели БД
│   │   ├── schemas/  # Pydantic схемы
│   │   ├── services/ # Бизнес-логика
│   │   └── core/     # Конфигурация
│   └── alembic/      # Миграции БД
├── frontend/         # React приложение
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   └── services/
│   └── package.json
└── docs/             # Документация
```

## Основные функции

✅ **Пожертвовать** - Единоразовые донаты с фильтрацией по фондам  
✅ **Поддержать** - Быстрые пожертвования по пресетам  
✅ **Подписка** - Регулярные пожертвования (садака-джария)  
✅ **Кампании** - Создание и поддержка целевых сборов  
✅ **Закят** - Калькулятор и выплата обязательного закята  
✅ **Фонды** - Каталог проверенных фондов-партнёров  
✅ **История** - История всех транзакций пользователя  

## Следующие шаги

1. **Интеграция платежных систем:**
   - Настройте YooKassa и CloudPayments
   - Добавьте реальные API ключи в `.env`
   - Реализуйте вебхуки для обработки платежей

2. **Добавление тестовых данных:**
   - Создайте несколько тестовых фондов через админ-панель
   - Добавьте примеры кампаний

3. **Оптимизация:**
   - Настройте кеширование
   - Добавьте индексы в БД
   - Оптимизируйте запросы

4. **Развертывание:**
   - Настройте CI/CD
   - Разверните на сервере
   - Настройте мониторинг

## Полезные ссылки

- [Документация API](docs/API.md)
- [Структура БД](docs/DATABASE.md)
- [Развертывание](docs/DEPLOYMENT.md)
- [Telegram Bot](docs/TELEGRAM_BOT.md)

## Поддержка

При возникновении проблем:
1. Проверьте логи backend и frontend
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте подключение к базе данных
4. Убедитесь, что порты 8000 и 3000 свободны

