# ⚙️ Быстрая настройка интеграции с e-replika.ru

## 📝 Шаг 1: Обновить .env файл

Добавьте в файл `backend/.env`:

```env
# E-Replika API интеграция
E_REPLIKA_API_URL=https://bot.e-replika.ru/api
E_REPLIKA_AUTH_TOKEN=test_token_123
```

## ✅ Шаг 2: Проверить зависимости

Убедитесь, что `httpx` установлен:

```bash
cd backend
pip install httpx
```

Или переустановите все зависимости:

```bash
pip install -r requirements.txt
```

## 🚀 Шаг 3: Запустить backend

```bash
cd backend
uvicorn app.main:app --reload
```

## 🧪 Шаг 4: Проверить интеграцию

### Через Swagger UI:

1. Откройте `http://localhost:8000/docs`
2. Найдите раздел **`statistics`**
3. Попробуйте эндпоинт:
   - `GET /api/v1/statistics/` - Общая статистика

### Через curl:

```bash
curl -X GET "http://localhost:8000/api/v1/statistics/" \
  -H "Authorization: Bearer test_token_123"
```

## 📊 Доступные эндпоинты

Все эндпоинты находятся в разделе `/api/v1/statistics/`:

- `GET /api/v1/statistics/` - Общая статистика
- `GET /api/v1/statistics/donations` - Статистика пожертвований
- `GET /api/v1/statistics/campaigns` - Статистика кампаний
- `GET /api/v1/statistics/users` - Статистика пользователей

## ⚠️ Важно

- **Токен**: Используется `test_token_123` для авторизации
- **URL**: `https://bot.e-replika.ru/api`
- Все запросы идут с заголовком `Authorization: Bearer test_token_123`

## 📚 Подробная документация

См. `E_REPLIKA_INTEGRATION.md` для полной документации.

---

**Готово!** ✅ Интеграция настроена и готова к использованию.

