# 🧪 Руководство по локальному тестированию

## ✅ Быстрая проверка

### 1. Frontend установлен ✅
Зависимости установлены успешно.

### 2. Запуск Frontend

```powershell
cd frontend
npm run dev
```

Frontend будет доступен на: **http://localhost:3000**

### 3. Настройка Backend

Сначала установите Python зависимости:

```powershell
cd backend

# Создайте виртуальное окружение (если еще не создано)
python -m venv venv
# или
py -m venv venv

# Активируйте
.\venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt
```

### 4. Настройка базы данных

⚠️ **Важно**: Перед запуском backend нужна база данных PostgreSQL.

#### Вариант A: У вас уже есть PostgreSQL

1. Создайте базу данных:
```sql
CREATE DATABASE sadakadb;
```

2. Обновите `backend/.env`:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/sadakadb
```

#### Вариант B: Используйте SQLite для тестирования (временно)

Можно временно изменить на SQLite для тестирования без PostgreSQL:

Измените `backend/.env`:
```env
DATABASE_URL=sqlite:///./test.db
```

⚠️ SQLite поддерживает не все функции, но для базового тестирования подойдет.

### 5. Создание миграций (опционально)

Если используете PostgreSQL:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

### 6. Запуск Backend

```powershell
cd backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload
```

Backend будет доступен на: **http://localhost:8000**
API документация: **http://localhost:8000/docs**

## 🚀 Быстрый запуск (2 терминала)

### Terminal 1 - Backend:
```powershell
cd backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload
```

### Terminal 2 - Frontend:
```powershell
cd frontend
npm run dev
```

## 🌐 Тестирование

1. Откройте **http://localhost:3000** в браузере
2. Вы должны увидеть главную страницу
3. Проверьте навигацию между страницами
4. Попробуйте открыть страницы:
   - /donate - Пожертвовать
   - /support - Поддержать
   - /campaigns - Кампании
   - /zakat - Калькулятор закята

## ⚠️ Важные замечания

### База данных

- Без базы данных API не будет работать полноценно
- Для быстрого тестирования UI можно запустить только frontend
- Backend вернет ошибки при попытке работы с БД без подключения

### API запросы

- Если backend не запущен, frontend покажет ошибки в консоли
- Откройте DevTools (F12) → Console для просмотра ошибок

### CORS

- Backend настроен для работы с localhost:3000
- Если используете другой порт, обновите CORS в `backend/app/main.py`

## 🔍 Проверка работы

### Frontend работает если:
- ✅ Страница открывается
- ✅ Навигация работает
- ✅ Нет ошибок в консоли (кроме API ошибок если backend не запущен)

### Backend работает если:
- ✅ http://localhost:8000/docs открывается
- ✅ http://localhost:8000/health возвращает `{"status": "ok"}`
- ✅ API отвечает на запросы

## 📝 Следующие шаги после успешного тестирования

1. Настроить реальную базу данных PostgreSQL
2. Создать первую миграцию
3. Добавить тестовые данные
4. Протестировать полный цикл пожертвований
5. Подготовить к деплою на Vercel

