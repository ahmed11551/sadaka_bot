# 🔗 Интеграция с API e-replika.ru

## 📋 Описание

Проект интегрирован с внешним API `bot.e-replika.ru` для получения статистики и синхронизации данных.

## 🔑 Авторизация

**Токен авторизации**: `test_token_123`

Токен передается в заголовке запроса:
```
Authorization: Bearer test_token_123
```

## ⚙️ Конфигурация

Настройки находятся в `backend/app/core/config.py`:

```python
E_REPLIKA_API_URL = "https://bot.e-replika.ru/api"
E_REPLIKA_AUTH_TOKEN = "test_token_123"
```

Можно переопределить через переменные окружения в `.env`:

```env
E_REPLIKA_API_URL=https://bot.e-replika.ru/api
E_REPLIKA_AUTH_TOKEN=test_token_123
```

## 📊 API Эндпоинты статистики

Все эндпоинты доступны по адресу: `/api/v1/statistics/`

### 1. Общая статистика

```
GET /api/v1/statistics/
```

**Параметры:**
- `start_date` (optional): Начальная дата (YYYY-MM-DD)
- `end_date` (optional): Конечная дата (YYYY-MM-DD)
- `group_by` (optional): Группировка (day, week, month)

**Примеры:**
```bash
# Статистика за последние 30 дней
GET /api/v1/statistics/

# Статистика за конкретный период
GET /api/v1/statistics/?start_date=2025-01-01&end_date=2025-01-31

# Статистика с группировкой по дням
GET /api/v1/statistics/?group_by=day
```

**Ответ:**
```json
{
  "period_start": "2025-01-01",
  "period_end": "2025-01-31",
  "data": {
    // Данные из e-replika API
  }
}
```

### 2. Статистика по пожертвованиям

```
GET /api/v1/statistics/donations
```

**Параметры:**
- `start_date` (optional): Начальная дата
- `end_date` (optional): Конечная дата

**Пример:**
```bash
GET /api/v1/statistics/donations?start_date=2025-01-01&end_date=2025-01-31
```

### 3. Статистика по кампаниям

```
GET /api/v1/statistics/campaigns
```

**Параметры:**
- `start_date` (optional): Начальная дата
- `end_date` (optional): Конечная дата

**Пример:**
```bash
GET /api/v1/statistics/campaigns?start_date=2025-01-01&end_date=2025-01-31
```

### 4. Статистика по пользователям

```
GET /api/v1/statistics/users
```

**Параметры:**
- `start_date` (optional): Начальная дата
- `end_date` (optional): Конечная дата

**Пример:**
```bash
GET /api/v1/statistics/users?start_date=2025-01-01&end_date=2025-01-31
```

## 🔄 Синхронизация данных

Сервис поддерживает синхронизацию данных с внешней базой e-replika:

### Синхронизация пожертвования

```python
from app.services.e_replika_service import e_replika_service

donation_data = {
    "id": 123,
    "amount": 1000,
    "user_id": 456,
    "fund_id": 789,
    "status": "completed",
    "created_at": "2025-01-01T12:00:00Z"
}

result = await e_replika_service.sync_donation(donation_data)
```

### Синхронизация кампании

```python
campaign_data = {
    "id": 123,
    "title": "Помощь сиротам",
    "goal_amount": 50000,
    "collected_amount": 30000,
    "status": "active"
}

result = await e_replika_service.sync_campaign(campaign_data)
```

### Синхронизация пользователя

```python
user_data = {
    "id": 456,
    "tg_id": 123456789,
    "first_name": "Иван",
    "username": "ivan_user"
}

result = await e_replika_service.sync_user(user_data)
```

## 📥 Получение данных из внешней базы

### Получить пользователей

```python
users = await e_replika_service.get_users(
    skip=0,
    limit=100,
    filters={"status": "active"}
)
```

### Получить пожертвования

```python
donations = await e_replika_service.get_donations(
    skip=0,
    limit=100,
    filters={"status": "completed"}
)
```

### Получить кампании

```python
campaigns = await e_replika_service.get_campaigns(
    skip=0,
    limit=100,
    filters={"status": "active"}
)
```

## 🛠️ Структура файлов

```
backend/
├── app/
│   ├── services/
│   │   └── e_replika_service.py    # Сервис для работы с API
│   └── api/
│       └── v1/
│           └── statistics.py       # API роутер для статистики
└── core/
    └── config.py                    # Конфигурация (токен, URL)
```

## 🔒 Безопасность

1. **Токен авторизации** хранится в переменных окружения
2. **HTTPS** обязателен для всех запросов
3. **Rate limiting** встроен в сервис (timeout 30 секунд)
4. **Логирование** всех ошибок запросов

## 📝 Логирование

Все запросы и ошибки логируются:

```python
import logging
logger = logging.getLogger(__name__)

# В логах будут:
# - HTTP ошибки (статус код, текст ответа)
# - Ошибки сети (timeout, connection error)
# - Неожиданные ошибки
```

## 🧪 Тестирование

### Локальное тестирование:

```bash
# Запустить backend
cd backend
uvicorn app.main:app --reload

# Проверить эндпоинт статистики
curl -X GET "http://localhost:8000/api/v1/statistics/" \
  -H "Authorization: Bearer test_token_123"
```

### Через Swagger UI:

1. Откройте `http://localhost:8000/docs`
2. Найдите раздел `statistics`
3. Выберите нужный эндпоинт
4. Нажмите "Try it out"
5. Заполните параметры (опционально)
6. Нажмите "Execute"

## ⚠️ Обработка ошибок

Сервис обрабатывает следующие типы ошибок:

1. **HTTP ошибки** (4xx, 5xx):
   ```python
   raise HTTPException(status_code=500, detail="Ошибка при получении статистики")
   ```

2. **Ошибки сети**:
   - Timeout (30 секунд)
   - Connection error
   - Все логируются в logger

3. **Ошибки валидации**:
   - Проверка формата дат
   - Проверка параметров запроса

## 🔄 План развития

- [ ] Добавить автоматическую синхронизацию при создании пожертвований
- [ ] Добавить кэширование статистики
- [ ] Добавить метрики производительности
- [ ] Добавить retry механизм для failed запросов
- [ ] Добавить webhook для получения обновлений от e-replika

## 📚 Дополнительная документация

См. официальную документацию API:
- **URL**: `https://bot.e-replika.ru/docs`
- **Токен**: `test_token_123`

## 💡 Примеры использования

### В коде бэкенда:

```python
from app.services.e_replika_service import e_replika_service

# Получить статистику за сегодня
from datetime import datetime
today = datetime.now().strftime("%Y-%m-%d")

stats = await e_replika_service.get_statistics(
    start_date=today,
    end_date=today
)

print(f"Статистика за сегодня: {stats}")
```

### В API эндпоинте:

```python
from fastapi import APIRouter
from app.services.e_replika_service import e_replika_service

router = APIRouter()

@router.get("/custom-stats")
async def get_custom_stats():
    stats = await e_replika_service.get_donations_statistics(
        start_date="2025-01-01",
        end_date="2025-01-31"
    )
    return stats
```

---

**Статус**: ✅ Интеграция реализована и готова к использованию

**Последнее обновление**: 2025-01-25

